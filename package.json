{
  "name": "recipe-book",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "(cd api && npm install) && (cd ui && npm install) && (cd e2e && npm install)"
  },
  "dependencies": {
    "docker": "^1.0.0",
    "make": "^0.8.1"
   
  }
}
